# 📅 Tag 3 — 28. April 2026

## 🎯 Was ich heute gemacht habe

- ModuleScript erstellt: `RarityModule` mit gewichtetem Zufallssystem
- Rarity-System mit 6 Stufen gebaut (Common bis Divine)
- `RarityModule.getRarityByName()` Funktion selbst geschrieben
- RarityModule von `ServerScriptService` nach `ReplicatedStorage` verschoben
- Lernplan für das Collector Game ins GitHub-Repo gepusht

## 💡 Was ich gelernt habe

### ModuleScripts
```lua
local MeinModule = {}

function MeinModule.meineFunktion()
    -- Logik hier
end

return MeinModule  -- MUSS die letzte Zeile sein!
```
Ein ModuleScript gibt immer ein Table zurück. Mit `require()` holt man es sich in anderen Scripts.

**C#-Vergleich:** Wie eine `static class` — nicht instanziierbar, direkt per `MeinModule.meineFunktion()` aufrufbar.

### Gewichtetes Zufallssystem
```lua
local roll = math.random(1, 100)
local counter = 0

for _, rarity in rarities do
    counter += rarity.weight
    if roll <= counter then
        return rarity  -- erste Rarity deren Zone die Zahl abdeckt
    end
end
```
Die Gewichte werden kumulativ addiert — jede Rarity "besitzt" einen Bereich der Zahlenreihe:
```
1–60   → Common     (60%)
61–85  → Rare       (25%)
86–95  → Epic       (10%)
96–99  → Legendary  ( 4%)
100    → Mythic      ( 1%)
```

### ReplicatedStorage vs. ServerScriptService
- `ServerScriptService` → nur Server
- `ReplicatedStorage` → Server **und** Client
- ModuleScripts die später auch vom Client gebraucht werden gehören in `ReplicatedStorage`

### `error()` vs. `nil` zurückgeben
In Luau gibt es kein `throw new Exception`. Äquivalent wäre `error("Nachricht")`.
Aber: wenn der Fehlerfall realistisch und handhabbar ist (z.B. Item nicht gefunden beim Laden), reicht `nil` zurückgeben — der Aufrufer prüft dann selbst.

**C#-Vergleich:** `throw` vs. `return null` — `throw` nur bei wirklich unerwartetem Fehler.

## 🐛 Bugs die ich selbst gefixed habe

- `local name = rarity` innerhalb von `getRarityByName()` → unnötige Zeile die den Parameter überschreibt, einfach gelöscht
- `getRarityByName()` nach `return RarityModule` platziert → Funktion wurde nie ausgeführt (rot markiert), vor das `return` verschoben
- `require(script.Parent.RarityModule)` nicht angepasst nach dem Verschieben → Pfad auf `game.ReplicatedStorage.RarityModule` geändert

## 🤔 Was ich verstanden habe (nach Nachfragen)

- Roll = 10 gibt **Common** zurück, nicht Epic — weil der Counter kumulativ zählt und bei 60 bereits `10 <= 60` gilt
- `return RarityModule` muss immer die **letzte Zeile** im ModuleScript sein — alles danach wird ignoriert
- `getRarityByName()` braucht kein `error()` weil der Name immer vom eigenen Code kommt (DataStore-Save), nicht vom User

## 🔗 Vergleich zu C#

| Luau | C# |
|---|---|
| `local M = {}` + `return M` | `static class M {}` |
| `require(game.ReplicatedStorage.M)` | `using M;` / Import |
| `math.random(1, 100)` | `Random.Next(1, 101)` |
| `error("msg")` | `throw new Exception("msg")` |
| `nil` zurückgeben | `return null` |

## ⏭️ Was kommt als nächstes

- Morgen: `ItemModule` selbst bauen (Items mit Rarity verknüpfen, nutzt `RarityModule.roll()` intern)
- Nächste Woche: Phase 2 — GUI, LocalScript, Roll-Button

---
**Stimmung:** 🟢 Motiviert — erstes richtiges ModuleScript selbst fertiggestellt!
